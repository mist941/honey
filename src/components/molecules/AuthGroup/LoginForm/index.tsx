import React, {ReactComponentElement} from 'react';
import styles from '../style.module.scss';

interface LoginFormProps {
}

export const AuthPage = ({}: LoginFormProps) => {
  return (
    <div>

    </div>
  );
};